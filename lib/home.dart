import 'package:flutter/material.dart';
import 'package:login_interface/screens/signInOne.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SignInOne(),
    );
  }
}